import React from 'react';

function Register () {
    function handleSubmit ( e ) {
        e.preventDefault();
    }

    return (
        <div>
            
        </div>
    );
}

export default Register;